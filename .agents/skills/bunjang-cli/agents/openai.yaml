interface:
  display_name: "번개장터 CLI 운영"
  short_description: "bunjang-cli 래퍼로 상점 운영 작업 수행"
  default_prompt: "Use $bunjang-cli to run a configured bunjang-cli wrapper operation with npm run bunjang -- <capabilityId> '<paramsJson>'."
policy:
  allow_implicit_invocation: true
