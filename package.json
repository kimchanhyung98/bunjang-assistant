{
  "name": "bunjang-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bunjang": "node src/index.js",
    "test": "node --test",
    "check": "npm test"
  },
  "dependencies": {
    "bunjang-cli": "0.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
